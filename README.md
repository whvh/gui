# Arcanum UI Library

![Version](https://img.shields.io/badge/version-1.0.0-blue.svg)
![License](https://img.shields.io/badge/license-MIT-green.svg)
![Roblox](https://img.shields.io/badge/platform-Roblox-red.svg)

Современная, многофункциональная библиотека GUI для Roblox эксплойтов с красивым дизайном и плавными анимациями.

## ✨ Особенности

- 🎨 **Современный дизайн** - Темная тема с настраиваемыми акцентными цветами
- 🌊 **Плавные анимации** - Система твининга с настраиваемыми параметрами
- 📱 **Адаптивность** - Полная поддержка мобильных устройств
- 🎛️ **Множество компонентов** - Тогглы, слайдеры, дропдауны, цветовые пикеры и многое другое
- 💾 **Система конфигураций** - Сохранение и загрузка настроек
- ⌨️ **Система кейбиндов** - Поддержка различных режимов (Toggle, Hold, Always)
- 🎯 **Цветовой пикер** - HSV цветовая модель с прозрачностью
- 🔧 **Настраиваемость** - Гибкая система тем и компонентов
- 📦 **Модульность** - Чистая архитектура для легкого расширения

## 📦 Установка

### Способ 1: Прямое подключение
```lua
local Arcanum = loadstring(game:HttpGet("YOUR_RAW_GITHUB_URL"))()
```

### Способ 2: Локальная установка
1. Скачайте файл `src/main.lua`
2. Загрузите его в ваш эксплойт
3. Вызовите библиотеку

## 🚀 Быстрый старт

```lua
-- Загрузка библиотеки
local Arcanum = loadstring(game:HttpGet("YOUR_RAW_GITHUB_URL"))()

-- Создание окна
local Window = Arcanum:Window({
    Name = "My Script",
    SubName = "v1.0.0",
    Logo = "rbxassetid://1234567890"
})

-- Создание страницы
local Main = Window:Page({
    Name = "Main", 
    Icon = "rbxassetid://1234567890"
})

-- Создание секции
local Section = Main:Section({
    Name = "Features",
    Side = 1
})

-- Добавление компонентов
Section:Toggle({
    Name = "Feature 1",
    Flag = "feature1",
    Default = false,
    Callback = function(Value)
        print("Feature 1:", Value)
    end
})

Section:Slider({
    Name = "Speed",
    Flag = "speed",
    Min = 1,
    Max = 100,
    Default = 50,
    Suffix = "km/h",
    Callback = function(Value)
        print("Speed:", Value)
    end
})

-- Инициализация
Window:Init()
```

## 📚 Документация

### Window

Основное окно интерфейса.

```lua
local Window = Arcanum:Window({
    Name = "Window Name",        -- Название окна
    SubName = "Subtitle",        -- Подзаголовок (опционально)
    Logo = "rbxassetid://..."    -- Иконка (опционально)
})
```

### Page

Страница с компонентами.

```lua
local Page = Window:Page({
    Name = "Page Name",          -- Название страницы
    Icon = "rbxassetid://...",   -- Иконка
    Columns = 2                  -- Количество колонок (опционально)
})
```

### Section

Секция для группировки компонентов.

```lua
local Section = Page:Section({
    Name = "Section Name",       -- Название секции
    Side = 1,                    -- Сторона (1 или 2)
    Description = "Description", -- Описание (опционально)
    Icon = "rbxassetid://..."    -- Иконка (опционально)
})
```

### Компоненты

#### Toggle
```lua
Section:Toggle({
    Name = "Toggle Name",
    Flag = "unique_flag",
    Default = false,
    Callback = function(Value)
        -- Код при изменении состояния
    end
})
```

#### Slider
```lua
Section:Slider({
    Name = "Slider Name",
    Flag = "unique_flag",
    Min = 0,
    Max = 100,
    Default = 50,
    Suffix = "%",
    Decimals = 1,
    Callback = function(Value)
        -- Код при изменении значения
    end
})
```

#### Dropdown
```lua
Section:Dropdown({
    Name = "Dropdown Name",
    Flag = "unique_flag",
    Items = {"Option 1", "Option 2", "Option 3"},
    Multi = false,               -- Множественный выбор
    Default = "Option 1",
    Callback = function(Value)
        -- Код при выборе
    end
})
```

#### Button
```lua
Section:Button({
    Name = "Button Name",
    Callback = function()
        -- Код при нажатии
    end
})
```

#### Textbox
```lua
Section:Textbox({
    Name = "Textbox Name",
    Flag = "unique_flag",
    Placeholder = "Enter text...",
    Numeric = false,            -- Только числа
    Finished = true,            -- Срабатывать при потере фокуса
    Callback = function(Value)
        -- Код при изменении текста
    end
})
```

#### Keybind
```lua
Section:Keybind({
    Name = "Keybind Name",
    Flag = "unique_flag",
    Default = Enum.KeyCode.X,
    Mode = "Toggle",             -- "Toggle", "Hold", "Always"
    Callback = function(Value)
        -- Код при активации
    end
})
```

#### Colorpicker
```lua
Section:Colorpicker({
    Name = "Colorpicker Name",
    Flag = "unique_flag",
    Default = Color3.fromRGB(255, 0, 0),
    Callback = function(Value)
        -- Код при изменении цвета
    end
})
```

#### Label
```lua
Section:Label({
    Name = "Label Name",
    RichText = false             -- Поддержка RichText
})
```

## 🎨 Темы

Библиотека поддерживает кастомные темы:

```lua
-- Изменение цвета акцента
Arcanum:ChangeTheme("Accent", Color3.fromRGB(0, 255, 127))

-- Изменение фонового цвета
Arcanum:ChangeTheme("Background", Color3.fromRGB(20, 20, 20))
```

### Доступные темы

- `Accent` - Основной акцентный цвет
- `AccentGradient` - Градиент акцента
- `Background` - Основной фон
- `Background 2` - Вторичный фон
- `Text` - Цвет текста
- `Outline` - Цвет границ
- `Element` - Цвет элементов
- `Section Top` - Верх секций
- `Section Background` - Фон секций
- `Section Background 2` - Альтернативный фон секций

## 💾 Конфигурации

Система сохранения/загрузки настроек:

```lua
-- Получение текущей конфигурации
local Config = Arcanum:GetConfig()

-- Загрузка конфигурации
Arcanum:LoadConfig(Config)

-- Удаление конфигурации
Arcanum:DeleteConfig("config_name.json")
```

## ⌨️ Горячие клавиши

- `RightControl` - Открыть/закрыть меню (по умолчанию)

## 🔧 Настройка

### Изменение ключа меню
```lua
Arcanum.MenuKeybind = tostring(Enum.KeyCode.Insert)
```

### Настройка анимаций
```lua
Arcanum.Tween.Time = 0.5                    -- Время анимации
Arcanum.Tween.Style = Enum.EasingStyle.Quad  -- Стиль анимации
Arcanum.Tween.Direction = Enum.EasingDirection.Out -- Направление
```

## 🐛 Отладка

Библиотека имеет встроенную систему отладки:

```lua
-- Включение отладки
Arcanum.Debug = true

-- Проверка флагов
print(Arcanum.Flags.your_flag)
```

## 📝 Примеры

Больше примеров использования можно найти в папке `examples/`.

## 🤝 Вклад

1. Fork проекта
2. Создайте ветку (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект лицензирован под MIT License - подробнее в файле [LICENSE](LICENSE).

## ⚠️ Предупреждение

Эта библиотека создана для образовательных целей. Используйте ее ответственно и в соответствии с правилами Roblox.

## 🙏 Благодарности

- Спасибо всем разработчикам Roblox за вдохновение
- Сообществу за поддержку и идеи

---

**Автор**: SuvCult 
**Версия**: 1.0.1 
**Последнее обновление**: 2024
